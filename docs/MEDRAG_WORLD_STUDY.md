# MedRAG: The Uncarved Answer

**Current owner authorization, 2026-09-09:** Build both complete illustrated worlds,
sequentially without agents. This supersedes the finite-proof proposal and historical
pauses below. Active previews are MedRAG / The Theatre of an Answer and SecondSelf /
A Little Further, Together. Versioned realistic alternatives are preserved. Read
docs/ILLUSTRATED_WORLDS_DELIVERY.md (ILLUSTRATED_WORLDS_DELIVERY.md from docs) for implementation
and verification. No production cutover is implied by local preview completion.


## Current animated build, 2026-09-08

The owner explicitly authorized creative execution, asset generation and animated worlds,
then SecondSelf. This supersedes the premise-selection hold. Work remains sequential,
without agents. The old paper-leaf treatment and static concept board are historical.

The active study is `design/directions/medrag-world.html`: an original generated basalt
chamber, eight moving evidence fragments, staged camera movement, citation lighting,
recorded refusal and a working two-query comparison. See `WORLD_GENERATED_ASSETS.md` for
the exact prompt, built-in generation method and portable workspace asset path.

### Design lock

The generated monolith is the concrete visual target. Preserve its weight, mineral detail,
dark chamber, empty centre and pale shaft of light. Use the audited Pudding persistent-subject
approach and Refero motion/craft guidance from earlier study work. The owner requested the
more immersive treatment and authorized direct creative choices. No fresh live Refero
research is claimed. Evidence fragments are code-native editorial geometry; their positions
are not embeddings or identities. The image provides material and place, not scientific data.

Eight chapters: arrival, bounded collection, query-1 retrieval, cited outcome, interactive
comparison, query-7 retrieval, refusal, evaluation ending. The query buttons animate between
two recorded outcomes without network requests. A shared bounded 30fps lifecycle coordinates
only scene events; the scene's material, controls and choreography are MedRAG-specific.
Complete narration and both records remain readable without JavaScript or with reduced motion.

No product calls, invented answer text, chunk identities, cause of refusal, or medical-safety
claim. No production route or schema registration is added at this visual checkpoint.

### Historical first study and evidence contract

**Rejected first treatment.** The owner requested new premises before another build.
See `MEDRAG_WORLD_EXPLORATION.md` and `design/directions/medrag-concepts.html`.
No replacement premise has been selected.

2026-09-07. Phase 6 standalone storyboard checkpoint. Owner selected this premise as a
storyboard basis; rendered storyboard approval and production implementation remain open.
No production route, world record, schema, dependency or published claim was changed.

## Story and reference lock

An optical reading surface receives translucent leaves. A retrieved set is spread out;
three cited leaves hold together. A second retrieved set has the same count, but its
leaves move aside and leave an answer plane blank. The ending gives that blank plane as
much space as the cited result. The graphic explains two recorded outcomes, without
impersonating a clinical interface or inventing an answer.

**Primary:** the Pudding persistent-graphic/progressive-disclosure method already audited
in `PHASE_2_BEATMIND_WORLD_RESEARCH.md`, particularly the documented music-DNA and onion
essays. Preserve one changing subject, native scrolling, bounded change and complete DOM
explanation. This study reuses the repository's documented research, not a new live audit.

**Secondary:** Tathya's tested scene-triggered events and settled holds. The earlier
scroll-scrub crawl is rejected. Local Refero `references/motion.md` supplies interruptible
ease-out transitions; `references/typography.md` supplies hierarchy, tracking and readable
measure. The locked project typefaces take precedence over that guide's generic presets.
No live Refero tools were available. No image generation is required for optical planes.

**Material lock:** the owner's selected translucent evidence leaves. Deep green-black exhibit ground
`#07110d`, pale ink `#eef5ef`, supporting ink `#b8cbc0`; mint `#4fd39b` marks
the cited subset, and a readable pale oxblood `#f39a8e` marks refusal. Light borders, surface refraction
and translucent overlapping planes are decorative. No gradients in type, dark dashboard
cards, embedding clouds, fabricated chunk text or constantly moving particles.

| Decision | Source and bounded role | Purpose |
|---|---|---|
| Persistent optical surface | Owner's Unwritten Answer premise + audited Pudding method | One visual subject connects both outcomes |
| Dark green-black optical exhibit | Retained dark-world lock; light confined to translucent planes | Distinct from the dark wood of Vivid and Tathya |
| Eight planes, then three accented | Committed query metadata | Represent counts only; no invented identities or rank |
| Blank right-hand answer plane | Recorded refusal plus owner-selected final beat | Make the boundary the visual climax |
| Reserved right column / portrait lower reading area | Audited portrait-recomposition research | Keep all narration clear of the sculpture |
| 440ms interrupted ease-out then stop | Local motion craft and Tathya timing lesson | Event legibility; duration is editorial, never inference latency |
| Bricolage / Archivo / DM Mono | DESIGN_LOCK | Display / narration / factual records retain their roles |

## Six beats

| Anchor | Narrative | Graphic and held state |
|---|---|---|
| `#arrival` | A bounded guidance collection | Overlapping translucent leaves suspended above a reading surface |
| `#question` | Recorded query 1 retrieves eight chunks | Eight leaves fan into two rows; no source identities implied |
| `#supported` | Query 1 cites three chunks and is not refused | Three green leaves gather; five recede |
| `#second-question` | Recorded query 7 also retrieves eight | The full count returns; the reader recognizes the same quantity |
| `#refusal` | Query 7 cites zero and is refused | Retrieved planes move away; a white answer plane remains empty |
| `#record` | Four refusals in the 20-case evaluation | Cited assembly left, blank outcome right; exact comparison in HTML |

The last frame is also the no-JavaScript and reduced-motion composition. The three small
rules on the cited-answer plane are an editorial indication, not actual answer text.
Under reduced motion the composed figure precedes the complete static narration; it does
not animate or change with scroll. Without JavaScript, the normal-flow figure uses the same
final composition while the complete narration remains accessible.

## Evidence contract

Verified read-only with `git show a741897:data/evaluation/ragas_results.json` in the
Evidence-Bound-Drug-RAG repository on 2026-09-07.

- Source commit: `a741897`.
- Source artifact: `data/evaluation/ragas_results.json`.
- Recorded timestamp: `2026-02-03T18:29:33.233479`.
- Evaluator: `RAGAS + OpenAI gpt-4o-mini`; retrieval method: `vector_only`.
- `metadata` query 1: `chunks_retrieved: 8`, `cited_chunks: 3`, `is_refusal: false`.
- `metadata` query 7: `chunks_retrieved: 8`, `cited_chunks: 0`, `is_refusal: true`.
- `num_queries: 20`; refused IDs 7, 11, 13 and 19, giving 4 of 20.

These records establish counts and recorded outcomes. They do not establish the mechanism
that selected citations, similarity distances, causal reasons for refusal, exact answer
wording, or clinical safety. The animatic makes none of those claims. The artifact has
latency fields, but the choreography does not display or reproduce them. The selected
query is labelled by recorded ID; no drug advice or unverified quotation appears.

The older embedding-radius proposal in `WORLDS.md` is replaced for this storyboard by
the owner's explicit reimagining request. In particular, the refused query **did retrieve
eight chunks**. An empty retrieval radius would misrepresent this evidence.

## Implementation and review

Files: `design/directions/medrag-world.html`, `.css`, `.js`. No external requests or
packages. Fonts reference the existing self-hosted files via `../../public/fonts/`.
The handoff targets `/work/medrag/` on the local research server's built-route mapping.

The script performs transform/opacity changes at a 30fps maximum, only for a 440ms scene
transition. It pauses on document hide and stops on page hide. Scroll directly chooses
the current section, so fast scrolling interrupts an unfinished event. Required words
and numbers stay in static HTML. No canvas exists, so Canvas failure cannot remove text.

Capture contract: narrative sections have `data-study-scene` plus `data-scene="0..5"`;
the figure has `data-study-stage` and `data-study-static` (one composed DOM sculpture,
enhanced in place); `#record` has `data-study-ending`. Screenshot arrival, supported,
refusal and record at 390, 800 and 1440 after a 600ms held-state wait. Inspect all six
at 390 for reserved-copy clearance, plus no-JavaScript/reduced-motion and a print render.

## Coordinated browser checkpoint: 2026-09-08

The combined capture run passed all 18 configurations across both studies: animated,
no-JavaScript and reduced-motion at 390/800/1440. It checked scene count, heading,
overflow, local assets, console errors and keyboard handoff. Representative rendered
arrival, turning point, ending and fallback frames were inspected. Evidence is in
`.shots/world-batch-1`. This supersedes the capture-pending note above.
Full performance and rendered contrast measurement are not claimed by this storyboard
check. Owner storyboard selection remains open.
