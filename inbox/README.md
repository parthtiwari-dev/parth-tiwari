# inbox/

Drop screenshots here and tell me the filename. Anything in this folder is raw
input, not a deliverable, and gets processed into `public/media/` before it is
used.

## Currently wanted

Two captures the automated browser session cannot produce. The stems waveforms
render as "loading" in an automated session even where they load normally for
the owner, so these have to come from a real browser by hand.

| File to save | What it is |
|---|---|
| `beatmind-stems.png` | The project page with VOCALS, DRUMS, BASS and OTHER waveforms drawn, plus BPM, key and the retune controls |
| `beatmind-arrangement.png` | ARRANGEMENT (68 bars), the section cards with chords and per-stem waveform lanes |

Full window is fine. The browser chrome gets cropped out during processing,
because the page frames these in its own window and two sets of chrome read as
a mistake.
